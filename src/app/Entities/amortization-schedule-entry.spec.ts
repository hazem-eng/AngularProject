import { AmortizationScheduleEntry } from './amortization-schedule-entry';

describe('AmortizationScheduleEntry', () => {
  it('should create an instance', () => {
    expect(new AmortizationScheduleEntry()).toBeTruthy();
  });
});
